# The Base Prompt

Codex will start reading from this prompt

- Codex will ignore the prompt where `status` is **Done**
- Codex will focus on the prompts where `status` is **Ongoing**


---
## prompt - 001 
`status` : **Done**
```
1. parent folder (using_claude) contains a web application made by you

```
```
Target folder:
    This folder

Important:
    Do not break or modify any existing logic unless it is directly required for the updates below.

Updates:
    1. Admin can update its password (using old password and new passawod), first need to validate via email otp
    2. Ater the game play, the before last 10 winner list, please show the current winner with system time, with chearing grafical emoji

```
---
---
## prompt - 002 
`status` : **Done**
```
1. parent folder (using_claude) contains a web application made by you

```
```
Target folder:
    This folder

Important:
    Do not break or modify any existing logic unless it is directly required for the updates below.

Updates:
    1. Make the cards drawing slower in tinpatti game in frontend, you can keep 5 seconds gaph
    2. User should able to update its passaword (using old password and new passaword) , this time no email otp validation or no email conformation will go
    3. Sill winner after game comoletion is not shown before the last 10 result row/ field, I do not need too much animation, just show a cup emoji and then Winner then name of the hand A or B, just make it little bit asthetic

```
---
---
## prompt - 003 
`status` : **Done**
```
1. parent folder (using_claude) contains a web application made by you

```
```
Target folder:
    This folder

Important:
    Do not break or modify any existing logic unless it is directly required for the updates below.

Updates:
    1. after User updates its passwd, show a message where user can see the new password. otherwise password can be forgotten

```
---
---
